# Application Configuration
PORT=5001

# Database Configuration
# Set DB_HOST to use PostgreSQL, leave unset for SQLite
DB_TYPE=postgresql
DB_HOST=db
DB_PORT=5432
DB_NAME=social_media
DB_USER=postgres
DB_PASSWORD=postgres

# For SQLite (when DB_HOST is not set):
# DB_PATH=social_media.db
