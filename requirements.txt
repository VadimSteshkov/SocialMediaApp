# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6  # For file uploads

# Database
psycopg2-binary>=2.9.9

# Message Queue
pika>=1.3.2  # RabbitMQ client

# Image processing (for API, but main processing in microservice)
Pillow>=10.0.0

# Testing dependencies
pytest>=7.4.0
pytest-html>=3.2.0
pytest-cov>=4.1.0
httpx>=0.25.0

# Python standard library modules are used:
# - sqlite3 (database, fallback)
# - datetime (timestamps)

